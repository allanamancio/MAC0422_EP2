#include <stdlib.h>
#include <stdio.h>





int main() {
	/*int i, j;
	int d = 20;
    int **circuito = (int**)malloc(10*sizeof(int*));
	if (circuito == NULL) return 0;
	for (i = 0; i < 10; i++) {
		circuito[i] = (int*)malloc(20*sizeof(int));
		if (circuito[i] == NULL) return 0;
		for (j = 0; j < d; j++)
			circuito[i][j] = 0;
	}*/
	int *g;
	g = malloc(sizeof(int));
	return 1;
}